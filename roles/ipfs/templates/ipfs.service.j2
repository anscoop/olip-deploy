[Unit]
Description=IPFS daemon
After=firewall
Wants=network-online.target


[Service]
Environment="IPFS_PATH={{ipfs_storage}}"
ExecStart=/usr/local/bin/ipfs daemon --mount
Restart=on-failure

[Install]
WantedBy=default.target
